class DirectionDetails{
  String? distanceText;
  String? durationText;
  int? distanceValue;
  String? durationValue;
  String? encodedpoint;
  DirectionDetails({
    this.distanceText, this.durationText, this.distanceValue, this.durationValue, this.encodedpoint
  });
  
}